import { $, read, money, shell, status, bindSearch, empty } from './core.js';
export function startListPage(config) { const data = read(); function rows(query = '') { const list = data[config.key].filter(item => Object.values(item).join(' ').toLowerCase().includes(query)); $('#list-rows').innerHTML = list.length ? list.map(item => `<tr>${config.columns.map(column => `<td class="${column.numeric ? 'numeric' : ''}">${column.format ? column.format(item) : item[column.key] ?? '—'}</td>`).join('')}</tr>`).join('') : `<tr><td colspan="${config.columns.length}">${empty(`No ${config.title.toLowerCase()} found.`)}</td></tr>`; } shell(config.page, config.title, config.description, `<div class="card toolbar"><div class="search-field"><input id="list-search" placeholder="Search ${config.title.toLowerCase()}"></div></div><div class="card table-card"><table class="table"><thead><tr>${config.columns.map(column => `<th class="${column.numeric ? 'numeric' : ''}">${column.title}</th>`).join('')}</tr></thead><tbody id="list-rows"></tbody></table></div>`, config.action || ''); bindSearch('#list-search', rows); rows(); }
