import { $, read, write, shell, toast } from './core.js';
let data = read();
shell('settings', 'Settings', 'Configure your company, inventory rules, and application data.', `<div class="card settings-section"><form id="settings-form" class="form-grid"><div class="field"><label>Company name</label><input name="company" value="${data.settings.company}"></div><div class="field"><label>Tax rate (%)</label><input name="tax" type="number" min="0" value="${data.settings.tax}"></div><div class="field"><label>Default minimum stock</label><input name="minimumStock" type="number" min="0" value="${data.settings.minimumStock}"></div><div class="field"><label>Allow negative stock</label><select name="allowNegative"><option value="false">No</option><option value="true" ${data.settings.allowNegative ? 'selected' : ''}>Yes</option></select></div><div class="field full"><label>Appearance</label><button class="toggle ${data.settings.dark ? 'on' : ''}" type="button" id="appearance-toggle"></button> Dark mode</div><div><button class="btn btn-primary">Save settings</button> <button type="button" class="btn btn-danger" id="reset-data">Reset data</button></div></form></div>`); $('#appearance-toggle').onclick = () => { data.settings.dark = !data.settings.dark; document.body.classList.toggle('dark', data.settings.dark); $('#appearance-toggle').classList.toggle('on', data.settings.dark); }; $('#settings-form').onsubmit = event => { event.preventDefault(); const values = Object.fromEntries(new FormData(event.currentTarget)); data.settings = { ...data.settings, ...values, tax: Number(values.tax), minimumStock: Number(values.minimumStock), allowNegative: values.allowNegative === 'true' }; write(data); toast('Settings saved.'); }; $('#reset-data').onclick = () => { localStorage.removeItem('stockflowData'); location.reload(); };
